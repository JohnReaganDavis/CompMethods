clc
close
clear

% Define the matrix A
A = [8, 2, -10; 
    -9, 1, 3; 
    15, -1, 6];

% Compute the 1-norm (maximum absolute column sum)
norm_1 = norm(A, 1);

% Compute the 2-norm (largest singular value of A)
norm_2 = norm(A, 2);

% Compute the infinity-norm (maximum absolute row sum)
norm_inf = norm(A, 'inf');

% Display the results
disp(['1-norm: ', num2str(norm_1)]);
disp(['2-norm: ', num2str(norm_2)]);
disp(['Infinity-norm: ', num2str(norm_inf)]);
